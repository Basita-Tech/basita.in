# Local development environment variables for email (DO NOT COMMIT)
# Replace placeholders with real values before testing

# Gmail SMTP account (recommended: create an App Password and use it as SMTP_PASS)
SMTP_USER=you@gmail.com
SMTP_PASS=your_gmail_app_password_here

# Optional (defaults shown in code)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# From and To addresses
EMAIL_TO=hr@basita.in